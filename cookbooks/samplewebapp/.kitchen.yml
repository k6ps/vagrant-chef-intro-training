---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-14.04
    driver_config:
      box: ubuntu/trusty32
    driver:
      customize:
        memory: 512

suites:
  - name: default
    run_list: [
      "recipe[samplewebapp]"
    ]
